package model;

/** A child class of User that provides functionality for a default user. */
public class DefaultUser extends AbstractUser {

    /** Constructs a DefaultUser object. */
    public DefaultUser() {
        super();
    }

    /** Creates a RegisteredUser object and registers the user with the system. */
    void register() {
    }
    
    /** Returns a RegisteredUser object 
}
